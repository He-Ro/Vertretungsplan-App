<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.hero.vertretungsplan"
    android:versionCode="5"
    android:versionName="@string/version_nr" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="11" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/CustomTheme" >
        <activity
            android:name="de.hero.vertretungsplan.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="de.hero.vertretungsplan.PrefsActivity"
            android:label="Optionen"></activity>
        <service android:name="de.hero.vertretungsplan.CheckForUpdates"></service>
        <service android:name="de.hero.vertretungsplan.WidgetUpdate"></service>
        <service android:name="de.hero.vertretungsplan.CheckForAppUpdate"></service>
		
		    <receiver android:icon="@drawable/ic_launcher"
              android:label="@string/app_name"
              android:name="WidgetProvider"
              android:enabled="@bool/honeycombOrAbove">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <meta-data android:name="android.appwidget.provider"
                 android:resource="@xml/widget_provider" />
    </receiver>
    <service android:name="WidgetService"
             android:permission="android.permission.BIND_REMOTEVIEWS" />
    
		<receiver android:name="OnBoot">
			<intent-filter>
				<action
				android:name="android.intent.action.BOOT_COMPLETED" />
				<!--  <category android:name="android.intent.category.HOME" />-->
			</intent-filter>
		</receiver>
    </application>

</manifest>